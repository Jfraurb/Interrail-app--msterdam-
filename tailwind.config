import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./app/**/*.{ts,tsx}", "./components/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
        // Deep canal water — primary dark surface
        canal: {
          DEFAULT: "#123A45",
          dark: "#0B262E",
          light: "#3E7C8C",
        },
        // Dutch bike-lane asphalt red — the "go" / signature accent
        fiets: {
          DEFAULT: "#B33F32",
          dark: "#8C2F25",
          light: "#D9695A",
        },
        // Gable-house ochre — warm highlight, used sparingly
        gevel: "#D8A24A",
        // Limestone / stone facades — light neutral background
        stone: {
          DEFAULT: "#EEEAE1",
          50: "#F7F5EF",
          100: "#EEEAE1",
          200: "#DFD9CB",
        },
        ink: "#10202A",
      },
      fontFamily: {
        display: ["var(--font-display)", "sans-serif"],
        body: ["var(--font-body)", "sans-serif"],
        mono: ["var(--font-mono)", "monospace"],
      },
      borderRadius: {
        node: "9999px",
      },
      boxShadow: {
        card: "0 1px 2px rgba(16,32,42,0.06), 0 4px 16px rgba(16,32,42,0.08)",
        nav: "0 -2px 12px rgba(16,32,42,0.10)",
      },
    },
  },
  plugins: [],
};
export default config;
